// Wrapper message for the SnapshotPB board configuration protocol.

syntax = "proto3";

import "device.proto";
import "profile.proto";

message Message {
    oneof payload {
        Device device = 1;
        Profiles profiles = 2;
    }
}